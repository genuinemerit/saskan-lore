# Design Backlog

A running list of deferred design decisions, technical debt, potential refactors,
and future feature candidates identified during development. Items here are not
requirements — they are candidates for future releases, each requiring its own
design and requirements work before implementation.

Add items here when something notable is deferred during a release rather than
letting it get lost in commit messages or conversation history.

---

## Items

| ID | Category | Summary | Origin |
| --- | --- | --- | --- |
| BL-001 | Debt | `load_entity_aliases()` and `load_relationships()` are no-ops — the current staging format (`ExtractionRecord`) has no `relationships` or alias fields. Both functions are wired and idempotent but called with empty lists. Requires staging format extension, extraction prompt update, and design work. | R4 |
| BL-002 | ~~Refactor~~ **Resolved (R5-prep)** | ~~`reviewed` / `status` vocabulary is inconsistent across staging and DB.~~ Resolved: `reviewed: bool` and staging-only `status: "rejected"` replaced by a single `review_status` enum (`pending`, `approved`, `rejected`) across all staging files, loader code, schema, and tests. DB `claims.status` vocabulary is unchanged and now matches exactly. | R4 |
| BL-003 | Design | Fuzzy entity matching (e.g. Levenshtein distance) to handle dialect spelling variants across lore texts. Needs threshold design, alias resolution strategy, ambiguity handling, and at minimum one ADR and one FR before implementation. | R4 |
| BL-004 | Debt | `_load_claim_entities()` in `load_reviewed.py` uses case-insensitive substring matching to link claims to entities. This is a heuristic — it can miss mentions and produce false positives. The `ClaimEntity.role` field is always NULL because the staging format provides no role data. A proper solution requires either NLP-based mention detection or explicit entity mention data in the staging format. | R4 |
| BL-005 | Debt | Entity type conflict is not detected. If an entity (e.g. "Varkaar") already exists in the DB with `entity_type="faction"` but a new staging file lists it under `places`, the loader silently returns the existing record with no warning. Consider logging a type mismatch or adding a review step. | R4 |
| BL-006 | Design | `reject_reason` is a staging-only field — it is logged at load time but not written to any DB column. If rejection audit history is valuable for analysis (e.g. tracking hallucination patterns), consider adding a `reject_reason` column to the `claims` table. Requires a schema migration. | R4 |
| BL-007 | Design | `extract_schema.json` covers extractor output only (`reviewed=false`). Post-review staging files (with `reviewed=true`, `status`, `reject_reason`) are not schema-validated. Consider whether a second JSON Schema for reviewed staging files would add value as documentation or for tooling. | R4 |
| BL-008 | Design | The extraction prompt and staging format do not currently produce relationship data or entity aliases. When this is addressed, `ExtractionRecord` in `data_schema.py`, `extract_schema.json`, the extraction prompt, and both loader stubs will all need coordinated updates. Worth designing as a single cohesive change rather than piecemeal. | R4 |
| BL-009 | Design | Embedding-based retrieval as an alternative to FTS5 keyword search. At load time, embed each claim using a local embedding model and store vectors as JSON blobs or in a `claim_embeddings` table. At query time, embed the query and compute cosine similarity (numpy) against stored vectors, returning top N by score. This is a drop-in replacement behind the existing `retrieve()` interface. Requires: embedding model selection (ADR), storage format decision, and similarity threshold design. Do not implement until FTS5 path is validated on real lore data. | R5 |
| BL-010 | Design | Conflict and contradiction extraction: during or after loading, flag claims that appear to contradict other claims already stored for the same entity or event. Distinct from `truth_status` labeling — requires cross-chunk comparison. No FR or ADR covers this. Candidate for a post-R6 release. | STATUS |
| BL-011 | Enhancement | Terminology consistency checking: regex-based scan for alternate spellings of entity names (e.g. "Varkaar" vs "Varkar"). `saskan_lore/analyzer/character_counter.py` is a stub that could anchor this work. No FR or design doc defines the intended scope. | STATUS |
| BL-012 | Enhancement | Entity frequency and co-occurrence analysis: count how often entities appear and how often they appear together. Useful for identifying central characters before the graph layer exists. `character_counter.py` is the relevant stub. No FR or design doc covers this. | STATUS |
| BL-013 | Design | Timeline as an explicit data element: the `era` field on `ChunkRecord` is free-text, not a structured temporal value. No schema field, FR, or ADR addresses how timeline or era data should be stored, queried, or presented. Worth designing before any graph or relationship layer is built. | STATUS |
| BL-014 | Design | DuckDB as optional analytical layer: complement to SQLite for aggregation, co-occurrence, and frequency queries. No ADR or release design covers when or how to introduce it. Candidate for an ADR when analytical needs become clear. | STATUS |
| BL-015 | Design | LoRA / PEFT fine-tuning path: fine-tune a small model (Qwen 3B or similar) on reviewed claims using LoRA/PEFT via MLX on Apple Silicon. ADR-001 defers this to post-MVP. No design doc covers training data format, evaluation criteria, or toolchain. Should be designed before the reviewed corpus grows too large to curate manually. | STATUS |
| BL-016 | Design | Two-assistant tool vision: (1) continuity assistant — "is this consistent with established lore?" used during active writing; (2) exploratory assistant — "tell me about X" for worldbuilding research. These are distinct products and may warrant separate design docs when retrieval is evaluated and stable. | STATUS |
| BL-017 | Design | Saskan console UI: long-term concept for a unified interface combining search, entity cards, timeline view, relationship graph, and continuity checker. Not relevant until retrieval is evaluated. No design work has been done. | STATUS |
| BL-018 | Design | Configurable scope tags: `"varkaar"` is currently hard-coded in `evaluate.py` (`_SCOPE`), `testing_schema.json` (enum), `register_lore_text.py`, and CLI help text. Expanding to multiple scopes (e.g. `["varkaar", "highweir"]`) requires a code change. Should be driven by config/env so a `scope-setup.sh` script or env variable can set the active scope list without touching source. Requires an ADR covering scope config format and validation before implementation. | R6 |
| BL-019 | Debt | `dba.summary()` reports a warning for FTS5 shadow tables (`claims_fts`, `claims_fts_config`, `claims_fts_data`, `claims_fts_docsize`, `claims_fts_idx`) that are not ORM models. These are expected SQLite-internal tables created by the FTS5 virtual table mechanism — not defects. Suppress by adding them to an allowlist in `dba.summary()` so the warning is not shown. Patch target: `v0.6.1`. | R6-accept |
| BL-021 | ~~Debt~~ **Resolved (v0.6.1)** | `DetachedInstanceError` in `ingest`, `extract`, `evaluate`, and `grade` CLI commands: ORM attributes accessed after session closes. Fixed by capturing values inside the `with get_session()` block or moving loops inside it. All four commands fixed in `saskan_lore/loader/ingest.py`. | R6-accept |
| BL-022 | Enhancement | PDF page numbers appear as isolated integers embedded mid-sentence in extracted text (e.g. "Then 68 the tannery workers."). Recommended fix: extract text page-by-page and strip the known page number token from each page's text before concatenating. This is precise — it removes only the integer that matches the current page number, leaving all other numeric content intact. Requires confirming whether the current PDF extractor already iterates page-by-page or reads the full document as one blob. Patch target: `v0.6.1`. | R6-accept |
| BL-027 | ~~Debt~~ **Resolved (v0.6.1)** | FTS5 retrieval returned 0 hits for natural-language questions: tokenizer built an implicit AND across all tokens including function words. Fixed by adding `_STOPWORDS` frozenset in `retrieval.py`; `tokenize()` filters stopwords before building the MATCH expression. | R6-accept |
| BL-028 | ~~Debt~~ **Resolved (v0.6.1)** | `grade` command unnecessarily loaded GGUF model on startup via module-level import chain. Fixed by moving `from saskan_lore.analyzer.answering import answer` to a deferred local import inside `run_evaluation()` in `evaluate.py`. | R6-accept |
| BL-026 | Enhancement | Richer reviewer feedback at review time: (1) add optional `correction_note` field when [C]orrect is chosen, parallel to `reject_reason` — lets reviewer record whether correction was a source typo, model paraphrase, or rephrasing; (2) make `reject_reason` more prominent at [R]eject — currently easy to skip, but reasons like `"source_span too vague"`, `"hallucination"`, or `"duplicate"` are valuable extraction quality signal. Both are staging-only fields; no schema migration needed. | R6-accept |
| BL-025 | ~~Debt~~ **Resolved (v0.6.1)** | `claim_text` vs `statement` field name mismatch: staging files use `"statement"` but `review_staging.py` read `claim.get('claim_text')` (blank display) and `load_reviewed.py` used `claim["claim_text"]` (all approved claims silently skipped). Fixed in both files and in test fixtures. | R6-accept |
| BL-024 | Enhancement | `review` command requires the user to type each staging file path manually. Enhance with an auto-advance mode: scan `var/reviewed/` for all `_extraction.json` files and step through them in order without requiring file path input. Could be a new `--auto` flag or a dedicated `review-all` command. Within a session, add `[N]ext` to advance to the next file and `[S]elect` to pick a specific file by name. Target: `v0.6.1`. | R6-accept |
| BL-023 | ~~Design~~ **Resolved (v0.6.1)** | Runtime state (`var/saskan_lore.db`, `var/reviewed/`) was inside the Dropbox-synced project root. Fixed: `scripts/setenv.sh` now derives `DATABASE_URL`, `REVIEWED_DIR`, and `LOG_DIR` from `SASKAN_VAR_DIR` (default `$HOME/.local/share/saskan-lore/`). Each machine gets an isolated runtime directory. `env.example` updated to document `SASKAN_VAR_DIR`. No Python source changes needed. | R6-accept |
| BL-020 | Design | macOS system acceptance test: repeat the full R6 system acceptance test on the Macintosh platform (Apple Silicon). Confirm pipeline, CLI commands, model loading, DB init, and FTS5 all behave correctly. Apply any short-term `v0.6.x` patches identified on Linux before running. Target: before `v1.0.0` graduation. | R6-accept |
| BL-029 | Debt | Repetitive/looping model output in `answer()` (and therefore `evaluate()`): `inference.complete()` calls llama.cpp with `temperature=0.1`, no `repeat_penalty` override (llama.cpp default is `1.0`, i.e. off), and no `stop` sequences, while sending a raw text-completion prompt to an instruct-tuned model (no ChatML turn-boundary tokens for it to anchor on). Once the model finishes a genuine answer it has no mechanical stop signal, and at near-zero temperature with no repetition penalty it falls into repeating its own conclusion verbatim until `max_tokens` truncates it mid-word. Observed on the macOS acceptance run (q_006, 3-claim context — longer natural answer left more token budget for the loop to develop); likely the same root cause as the "verbose/repetitive" answer noted during the Linux run's Phase 5 `ask` spot-check. Fix candidates: set `repeat_penalty` (~1.1–1.3), add a `stop` sequence, and/or switch to Qwen's chat template via `create_chat_completion()` instead of raw `complete()`. | R6-accept |
| BL-030 | Design | Sequential review order biases eval topic coverage: both the Linux and macOS acceptance runs reviewed chunks sequentially from `chunk_0001` forward and stopped once the 100–200 approved-claim target was reached, so both runs ended up covering roughly the same early slice of the document. The 10 Varkaar eval questions span topics across the whole corpus, so questions about later-document topics get zero retrieval hits on both runs regardless of model quality — the *same* 4 of 10 questions (q_005, q_006, q_009, q_010) were the only ones with any retrieved evidence on both platforms. Reaching the 7/10 graduation threshold likely requires either reviewing much more of the corpus, or changing review strategy to sample chunks spread across the document (e.g. every Nth chunk) instead of strictly sequential, so the reviewed subset's topic coverage better matches the eval question set. | R6-accept |
| BL-031 | ~~Debt~~ **Resolved (v0.6.2)** | `eval_summary()` and `export_results()` in `evaluate.py` counted every `EvalResult` row ever written, with no "latest per question" filter — directly contradicting the documented re-run workflow ("Re-run `saskan-lore evaluate` after the fix; previous results are preserved," `system_test.md`). Re-running `evaluate()` after a pipeline fix doubled the apparent question count and orphaned old grades. Fixed by adding `_latest_result_ids()` (group by `question_id`, take `max(id)`) and filtering both functions to it. Separately, `export-eval`'s default output path (`saskan_lore/loader/ingest.py`, `export_eval_cmd`) was hardcoded to `var/eval_export_<timestamp>.json` — a relative path that landed inside the Dropbox-synced repo root, bypassing the `SASKAN_VAR_DIR` scheme BL-023 established. Fixed to read `SASKAN_VAR_DIR` from the environment, falling back to `var` only if unset. | R6-accept |
| BL-032 | ~~Debt~~ **Resolved (v0.6.2)** | `claims_fts`'s default FTS5 tokenizer (`unicode61`, no stemming) caused exact-token AND matching in `retrieve()` to fail whenever a query word and the indexed claim text differed only by inflection (e.g. question "function" vs claim "functions", question "accept" vs claim "accepts"). Confirmed on the macOS acceptance run: q_002 and q_003 had zero retrieval hits despite an exact-match approved claim existing for each. Fixed via Alembic migration `0e99af659a1b` — drops and recreates `claims_fts` with `tokenize='porter unicode61'` (Porter stemming), then rebuilds the index from `claims`. `tests/conftest.py`'s in-memory FTS5 fixture updated to match. | R6-accept |

---

## Categories

| Label | Meaning |
| --- | --- |
| Design | Needs ADR and/or FR before implementation |
| Refactor | Code improvement with no new functionality |
| Enhancement | Small improvement, minimal design needed |
| Debt | Currently a stub, approximation, or known limitation |
